import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Jeu-de-Piste-Kt-Builder',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
